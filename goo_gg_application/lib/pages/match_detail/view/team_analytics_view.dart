import 'package:flutter/material.dart';

class TeamAnalyticsView extends StatelessWidget {
  const TeamAnalyticsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('team analytics');
  }
}
